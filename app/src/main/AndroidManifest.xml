<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.greg.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OC Tranpso Activities -->

        <activity android:name="com.example.greg.octranspo.MainActivity" />
        <activity android:name="com.example.greg.octranspo.SearchStopsActivity" />
        <activity android:name="com.example.greg.octranspo.StopDetailsActivity" />
        <activity android:name="com.example.greg.quizgame.QuizActivity" />
        <activity
            android:name="com.example.greg.quizgame.MessageDetails"
            android:theme="@style/Theme.AppCompat" />
        <activity android:name="com.example.greg.patientintake.PatientActivity" />
        <activity android:name="com.example.greg.movieform.MovieActivity"></activity>
    </application>

</manifest>